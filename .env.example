# Server
PORT=8080
PUBLIC_BASE_URL=https://party.example.com
LAN_BASE_URL=https://party.home.arpa
# Nur dem lokalen Reverse Proxy vertrauen. Bei fester Adresse besser exakt setzen,
# z. B. TRUST_PROXY=192.168.66.30
TRUST_PROXY=loopback,linklocal,uniquelocal

# Spotify Developer Dashboard
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
SPOTIFY_REDIRECT_URI=https://party.example.com/api/admin/spotify/callback

# Mit `openssl rand -base64 48` jeweils neu erzeugen
SESSION_SECRET=
ENCRYPTION_KEY=
HEALTHCHECK_TOKEN=

# Nur beim allerersten Admin-Login erforderlich
ADMIN_SETUP_TOKEN=

# Optional für Entwicklung und automatisierte Tests ohne Spotify
DEMO_MODE=false

# Optional: Controller-Takt (mindestens 10 Sekunden zum Schutz des Spotify-Kontingents)
# und Vorlauf bis zum Festschreiben des nächsten Songs
CONTROLLER_INTERVAL_MS=10000
LOCK_BEFORE_END_MS=30000

# Gemeinsames Minutenbudget für nicht gecachte Spotify-Aufrufe aller Gäste.
# Admin und Controller verwenden eine priorisierte, getrennte Spur.
GUEST_SPOTIFY_REQUESTS_PER_MINUTE=20
